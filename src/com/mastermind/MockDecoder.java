/*
package com.mastermind;

import java.util.Arrays;
import java.util.List;

public class MockDecoder extends Decoder {
    private List<List<Color>> orderedGuesses;
    public setGuesses(List<List<Color>> guesses) {
        Arrays.asList(Color.red, Color.blue, Color.green, Color.yellow);
        Arrays.asList(Color.blue, Color.red, Color.green, Color.green);
        Arrays.asList(Color.green, Color.green, Color.blue, Color.green);
        Arrays.asList(Color.green, Color.green, Color.green, Color.green);
    }

    @Override
    public List<Color> promptGuess(List<Turn> turns, int size) {

    }
}
*/
